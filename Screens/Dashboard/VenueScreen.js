import React from 'react';
import {
  StyleSheet
} from 'react-native';

const VenueScreen = () => {
  return(
        <>
        </>
  );
};

const styles = StyleSheet.create({
});

export default VenueScreen;
